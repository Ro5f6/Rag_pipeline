fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sentence-transformers==3.1.1
faiss-cpu==1.9.0.post1
rank_bm25==0.2.2
langchain-text-splitters==1.1.2
pypdf==5.1.0
numpy<2.0

# Generation goes through LangChain's init_chat_model (pipeline/llm_generate.py).
# `langchain` provides init_chat_model; each provider ships its own integration
# package, which init_chat_model imports lazily for the configured provider.
langchain==1.3.17
langchain-groq==1.1.3            # Groq: fast hosted open-source models (default)
langchain-ollama==1.1.0         # Ollama: local open-source models
langchain-openai==1.6.0         # OpenAI + any OpenAI-compatible server (vLLM, Together, Fireworks, OpenRouter) via base_url
langchain-google-genai==4.3.5   # Google Gemini

# Generation evaluation (RAGAS). Only needed to run `make eval-gen`, not to serve.
# langchain-community is required because ragas 0.4.x still imports a Vertex model
# from it at load time (patched by evaluation/_ragas_compat.py).
ragas==0.4.3
langchain-community==0.4.2

# Test / dev
pytest==8.3.3
httpx==0.28.1
