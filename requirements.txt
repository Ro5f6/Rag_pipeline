fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sentence-transformers==3.1.1
faiss-cpu==1.9.0.post1
rank_bm25==0.2.2
langchain-text-splitters==0.3.8
numpy<2.0

# Generation providers -- both optional. With neither installed or configured,
# the pipeline uses the offline extractive backend (see pipeline/llm_generate.py).
# anthropic must stay >=0.40: older releases pass `proxies=` to httpx, which
# httpx 0.28 removed, so the client raises TypeError the moment a key is set.
anthropic==0.122.0
# openai==1.51.0   # uncomment for OpenAI / vLLM / Ollama / Groq / Together

# Test / dev
pytest==8.3.3
httpx==0.28.1
